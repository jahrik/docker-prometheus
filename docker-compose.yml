version: '3'

services:

  prometheus:
    image: jahrik/arm-prometheus:arm32v7
    ports:
      - 9090:9090
    volumes:
      - /mnt/g2/prometheus:/prometheus
    deploy:
      mode: replicated
      replicas: 1
